/**
 * Top Nav Bar Slot
 *
 * Custom component rendered in the center of the top nav bar,
 * between the hamburger menu and the theme/user controls.
 *
 * This file is NOT synced from template — it's owned by your project.
 * Return null to leave the slot empty.
 */

import type { ReactNode } from 'react';

export const TopNavBarSlot = (): ReactNode => null;
