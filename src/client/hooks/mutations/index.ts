/**
 * React Query mutation hooks for modifying data
 * 
 * Note: Route-specific hooks are colocated with their routes.
 * This file contains only shared/cross-route mutation hooks.
 */

export {
    useLogin,
    useRegister,
    useLogout,
} from './useAuthMutations';
